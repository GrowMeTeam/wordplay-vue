android {
            defaultConfig {
                applicationId = "org.nativescript.application"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }